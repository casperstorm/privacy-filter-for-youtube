# Privacy Filter for Youtube

Small Firefox extension that adds a `Hide video` button on YouTube watch pages. Clicking it places a black overlay over the video player so audio keeps playing without showing the video.

## Load in Firefox

1. Open `about:debugging#/runtime/this-firefox`
2. Click `Load Temporary Add-on...`
3. Select `manifest.json`
